org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# AGP 8.1's androidJdkImage transform (jlink) can't read JDK 21's module
# metadata ("ModuleTarget is malformed") — build with 17 instead until AGP
# is upgraded past 8.2.1, which fixed this on the AGP side.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.20.8-hotspot
